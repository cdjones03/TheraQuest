using UnityEngine;

public class MasterPill : MonoBehaviour
{
    // directory of where the pill teleports should go

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
